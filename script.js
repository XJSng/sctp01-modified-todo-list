document.addEventListener("DOMContentLoaded", function () {
    function main() {
       /* 
        function renderitems(packingList) {
            const packingContainer = document.querySelector("#packingContainer");
            packingContainer.innerHTML = '';
            for (let each of packingList) {
                const div = document.createElement('div');
                div.className = 'd-flex flex-wrap align-items-center p-2 border border-dark'
                div.innerHTML = `${each.name} <span class="badge bg-primary">${each.clothesType}</span>`
            packingContainer.appendChild(div)   
            }
            
        }
        */
    }
    //  Call main function
    
    main()
})